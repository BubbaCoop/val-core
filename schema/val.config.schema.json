{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/BubbaCoop/val-core/schema/val.config.schema.json",
  "title": "Val library configuration (val/config.json)",
  "description": "Everything that differs between Valiify component libraries. val-init substitutes these values into the shared agent templates. Anything NOT in here is shared methodology and belongs in the templates or the core skills.",
  "type": "object",
  "required": [
    "library",
    "paths"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string"
    },
    "library": {
      "type": "object",
      "required": [
        "name",
        "displayName",
        "figmaFileKey"
      ],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Machine name — used in run ids and generated headers.",
          "pattern": "^[a-z][a-z0-9-]*$",
          "examples": [
            "shortapp",
            "dashboard"
          ]
        },
        "displayName": {
          "type": "string",
          "description": "Human name used in agent prose ('the Short App type system…').",
          "examples": [
            "Short App",
            "Dashboard"
          ]
        },
        "figmaFileKey": {
          "type": "string",
          "description": "The 22-char key from the Figma file URL.",
          "pattern": "^[A-Za-z0-9]{22}$"
        },
        "figmaFileName": {
          "type": "string",
          "description": "Optional — the Figma file's title, for prose only."
        },
        "package": {
          "type": "string",
          "description": "The npm package name consumers install (e.g. @valiify/shortapp-ui). Used by the design pipeline for @import lines and node_modules paths.",
          "examples": [
            "@valiify/shortapp-ui",
            "@valiify/dashboard-ui"
          ]
        }
      }
    },
    "paths": {
      "type": "object",
      "required": [
        "designSystemSkill"
      ],
      "additionalProperties": false,
      "description": "All repo-relative. Defaults match the Valiify library layout; override only where a repo differs.",
      "properties": {
        "designSystemSkill": {
          "type": "string",
          "description": "The library-specific design-language skill (components, tokens, casing rules). Stays in the library repo — never in val-core.",
          "examples": [
            ".claude/skills/valiify-shortapp-ui/SKILL.md"
          ]
        },
        "componentRegistry": {
          "type": "string",
          "default": "val/registry/components.json"
        },
        "tokenSource": {
          "type": "string",
          "default": "src/themes/valiify.css",
          "description": "Generated theme CSS the build agent reads token names from."
        },
        "componentCss": {
          "type": "string",
          "default": "dist/index.css",
          "description": "The built library the val page consumes."
        },
        "iconSprite": {
          "type": "string",
          "default": "src/icons/sprite.svg"
        },
        "storiesDir": {
          "type": "string",
          "default": "stories/components",
          "description": "Storybook stories the registry generator scans; also the staleness reference for the registry."
        },
        "componentsDir": {
          "type": "string",
          "default": "src/components"
        },
        "runOutputDir": {
          "type": "string",
          "default": "val/runs"
        },
        "toolsDir": {
          "type": "string",
          "default": "val/tools",
          "description": "Where val-init symlinks the core tools."
        },
        "figmaTokensJson": {
          "type": "string",
          "default": "tokens/figma-tokens.json",
          "description": "Extract pipeline: the token source-of-truth new variables are added to."
        },
        "buildThemeScript": {
          "type": "string",
          "default": "scripts/build-theme.mjs"
        },
        "visualSpecsScript": {
          "type": "string",
          "default": "scripts/visual-specs.mjs"
        },
        "componentProcessDoc": {
          "type": "string",
          "default": "docs/component-process.md"
        },
        "claudeMdSections": {
          "type": "string",
          "default": "Design Tokens + Quick Reference",
          "description": "Which CLAUDE.md sections the build agent must read fresh."
        },
        "qaProbes": {
          "type": "string",
          "description": "Optional. An ESM module exporting library-specific QA probe hooks (hoverExclude, focusTargetMap, settleMs, menus, disclosures) that tools/qa/standing.mjs merges under the page's own config. Generic Playwright plumbing stays in val-core; only what is specific to this library's components goes here.",
          "examples": [
            "val/qa-probes.mjs"
          ]
        },
        "libraryRoot": {
          "type": "string",
          "default": ".",
          "description": "Where the library's CLAUDE.md, GETTING_STARTED.md and src/ live. \".\" in the library repo; node_modules/<package> in a consumer app.",
          "examples": [
            ".",
            "node_modules/@valiify/shortapp-ui"
          ]
        }
      }
    },
    "typography": {
      "type": "object",
      "additionalProperties": false,
      "description": "The one area where the libraries' design systems genuinely diverge in agent behaviour.",
      "properties": {
        "fonts": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "@fontsource/inter"
          ],
          "description": "Font packages the built page imports.",
          "examples": [
            [
              "@fontsource/inter"
            ],
            [
              "@fontsource/inter",
              "@fontsource/jetbrains-mono"
            ]
          ]
        },
        "systemNote": {
          "type": "string",
          "description": "One sentence appended after the font list in the build agent — the type-system constraint it must respect.",
          "examples": [
            "the Short App type system is INTER-ONLY (all 24 text styles; there is no mono register in this design system)",
            "Inter for UI, JetBrains Mono strictly for verifiable data"
          ]
        },
        "dataRule": {
          "type": "string",
          "description": "How verifiable data (IDs, amounts, percentages) is set. Substituted verbatim into val-build rule 4 and val-context §4.",
          "examples": [
            "tabular-nums (the system is Inter-only; no mono register exists)",
            "JetBrains Mono + tabular-nums"
          ]
        }
      }
    },
    "audience": {
      "type": "string",
      "description": "Sentence for val-context §1 describing which audiences/registers this library serves and how that selects component families.",
      "examples": [
        "applicant-facing (the Short App application flow — fields, cards, actions) or portal/reviewer-facing (the Portal Specific set: StatusTracker, Action, UtilityButton). This selects which component families apply.",
        "reviewer-facing or applicant-facing (this selects the design language register)."
      ]
    },
    "pipelines": {
      "type": "object",
      "additionalProperties": false,
      "description": "Which pipelines to generate. val and extract default to true; design defaults to false and needs a design.surfaces entry.",
      "properties": {
        "val": {
          "type": "boolean",
          "default": true,
          "description": "Page assembly: /val + val-* agents."
        },
        "extract": {
          "type": "boolean",
          "default": true,
          "description": "Component extraction: /extract + extract-* lanes."
        },
        "design": {
          "type": "boolean",
          "default": false,
          "description": "Brief → concept → human gate → Svelte handoff: /design + design-* agents. Requires design.surfaces."
        }
      }
    },
    "design": {
      "type": "object",
      "additionalProperties": false,
      "description": "Design pipeline configuration. A surface is a page-composition methodology (e.g. Short App, Dashboard); one library may serve several.",
      "properties": {
        "methodologyDir": {
          "type": "string",
          "default": "design-methodology",
          "description": "Directory holding the surface methodology files. In the library repo: design-methodology. In a consumer app: node_modules/<package>/design-methodology (the library ships the folder)."
        },
        "surfaces": {
          "type": "object",
          "description": "Map of surface id → surface. The /design command takes --surface <id>; with one entry it is the default.",
          "additionalProperties": {
            "type": "object",
            "required": [
              "displayName",
              "methodology"
            ],
            "additionalProperties": false,
            "properties": {
              "displayName": {
                "type": "string",
                "examples": [
                  "Short App"
                ]
              },
              "methodology": {
                "type": "string",
                "description": "Methodology file name inside methodologyDir. Its §10 'shipped but unused' line and §12 interim-class column are read by class-audit; its companion *-decisions.md is history and is never read.",
                "examples": [
                  "short-app.md"
                ]
              },
              "flows": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Flow ids the brief may name.",
                "examples": [
                  [
                    "consumer",
                    "business",
                    "auto-loan"
                  ]
                ]
              },
              "viewports": {
                "type": "array",
                "description": "Viewports a concept must draw — one <main data-concept data-viewport=id> each.",
                "items": {
                  "type": "object",
                  "required": [
                    "id"
                  ],
                  "additionalProperties": false,
                  "properties": {
                    "id": {
                      "type": "string"
                    },
                    "note": {
                      "type": "string"
                    }
                  }
                }
              },
              "regions": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Region vocabulary for data-region on concept blocks (the surface's shell parts)."
              },
              "briefSchema": {
                "type": "array",
                "description": "01-brief.md body headings in order; required ones are stop triggers when the brief lacks them. The trailer (Source map, Open questions, Rules applied, Unsure) is always appended.",
                "items": {
                  "type": "object",
                  "required": [
                    "heading"
                  ],
                  "additionalProperties": false,
                  "properties": {
                    "heading": {
                      "type": "string"
                    },
                    "hint": {
                      "type": "string"
                    },
                    "required": {
                      "type": "boolean"
                    }
                  }
                }
              },
              "stopTriggers": {
                "type": "array",
                "description": "Surface-specific BLOCKING stop triggers, in addition to the five generic ones.",
                "items": {
                  "type": "object",
                  "required": [
                    "trigger",
                    "when"
                  ],
                  "additionalProperties": false,
                  "properties": {
                    "trigger": {
                      "type": "string"
                    },
                    "when": {
                      "type": "string"
                    }
                  }
                }
              },
              "conceptSections": {
                "type": "array",
                "items": {
                  "type": "string"
                },
                "description": "Sections concept.md opens with, in order, before the generic ones."
              }
            }
          }
        },
        "output": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "framework": {
              "type": "string",
              "default": "sveltekit",
              "description": "Handoff target framework."
            },
            "svelteMajor": {
              "type": "string",
              "default": "5",
              "description": "Svelte major version the handoff targets (5 = runes)."
            },
            "pagesDir": {
              "type": "string",
              "default": "src/routes",
              "description": "Where route files land inside the package."
            },
            "componentsDir": {
              "type": "string",
              "default": "src/lib/components",
              "description": "Where composed components land inside the package."
            }
          }
        }
      }
    }
  }
}
